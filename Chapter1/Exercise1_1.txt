MSVC (Microsoft Visual C++) Compiler accepts files with names that follow FAT, HPFS, or NTFS naming conventions. 
The filename extension determines how files are processed. C and C++ files, which have extension .c, .cxx or .cpp are compiled. 