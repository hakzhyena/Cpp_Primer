Exercise 3.29: List some of the drawbacks of using an array instead of a vector.

Ans:
you need to know the size of the array at compile time because array can't grow dynamically.
declarations can get quite complicated.